<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    
   
        <script src="https://cdn.tailwindcss.com"></script>
    
    
</head>
<body>
    <div class="">
    <%- include("partials/header.ejs") %>
    <div class="flex">
        <div class="flex flex-col bg-gradient-to-b from-violet-400 to-violet-600 w-1/5 h-screen items-center p-6 shadow-lg rounded-r-2xl">
            <!-- Logo -->
            <div class="mb-6">
                <h1 class="text-white text-2xl font-bold">My Blog</h1>
            </div>
        
            <!-- Navigation Links -->
            <nav class="flex flex-col w-full space-y-4">
                <a href="/dashboard" class="w-full text-center text-white py-3 rounded-lg bg-violet-500 hover:bg-violet-700 transition duration-300 shadow-md">
                    Dashboard
                </a>
                <a href="/create" class="w-full text-center text-white py-3 rounded-lg bg-pink-500 hover:bg-pink-600 transition duration-300 shadow-md">
                    Create Post
                </a>
                <a href="/profile" class="w-full text-center text-white py-3 rounded-lg bg-blue-500 hover:bg-blue-600 transition duration-300 shadow-md">
                    Profile
                </a>
                <a href="/settings" class="w-full text-center text-white py-3 rounded-lg bg-green-500 hover:bg-green-600 transition duration-300 shadow-md">
                    Settings
                </a>
            </nav>
        
            <!-- Logout Button -->
            <div class="mt-auto w-full">
                <a id="logout" href="/logout" class="w-full text-center text-white py-3 rounded-lg bg-red-500 hover:bg-red-600 transition duration-300 shadow-md">
                    Logout
                </a>
            </div>
        </div>
        
            
           <div class="w-full md:w-3/4 lg:w-1/2 mx-auto p-6 overflow-y-auto h-screen space-y-6">
    <% blogs.forEach(blog => { %>
        <div class="p-6 bg-white rounded-2xl shadow-lg transition-transform duration-300 hover:scale-105 hover:shadow-2xl">
            <h2 class="text-2xl font-bold text-gray-900 mb-2"><%= blog.title %></h2>
            <p class="text-gray-700 leading-relaxed mb-3"><%= blog.content %></p>
            <p class="text-sm text-gray-500 mb-4 italic"><%= blog.date %></p>
            
            <form action="/delete/<%= blog.id %>" method="POST">
                <button type="submit" 
                    class="w-full md:w-auto bg-red-500 text-white px-5 py-2 rounded-lg hover:bg-red-600 transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-red-400 focus:ring-offset-2">
                    Delete
                </button>
            </form>
        </div>
    <% }) %>
</div>

           


    </div>



    
    <%- include("partials/footer.ejs") %>
    </div>
</body>
</html>